{
  "sksl": {
    "entrypoint": "lighting_fragment_main",
    "shader": "// This SkSL shader is autogenerated by spirv-cross.\n\nfloat4 flutter_FragCoord;\n\nuniform vec2 screenSize;\nuniform vec3 lightPos;\nuniform float heightScale;\nuniform shader albedoMap;\nuniform half2 albedoMap_size;\nuniform shader depthMap;\nuniform half2 depthMap_size;\n\nvec4 fragColor;\n\nvec2 FLT_flutter_local_FlutterFragCoord()\n{\n    return flutter_FragCoord.xy;\n}\n\nvec3 FLT_flutter_local_calculateBasicLighting()\n{\n    vec2 uv = FLT_flutter_local_FlutterFragCoord() / screenSize;\n    vec4 depthMapPixel = depthMap.eval(depthMap_size * ( uv));\n    vec2 nxy = (depthMapPixel.xy * 2.0) - vec2(1.0);\n    float nz = sqrt(max(0.0, (1.0 - (nxy.x * nxy.x)) - (nxy.y * nxy.y)));\n    vec3 n = normalize(vec3(nxy, nz));\n    float height = depthMapPixel.z;\n    vec3 fragPos = vec3(uv, height);\n    vec3 L = lightPos - fragPos;\n    float dist = length(L);\n    L = normalize(L);\n    float NdotL = max(dot(n, L), 0.0);\n    float range = 20.200000762939453125;\n    float att = 1.0 / (1.0 + ((16.0 * (dist / range)) * (dist / range)));\n    vec3 lightColor = vec3(0.0, 0.20000000298023223876953125, 1.0);\n    vec3 diffuse = ((lightColor * NdotL) * att) * 3.0;\n    return diffuse;\n}\n\nvoid FLT_main()\n{\n    vec2 uv_1 = FLT_flutter_local_FlutterFragCoord() / screenSize;\n    vec4 albedoPixel = albedoMap.eval(albedoMap_size * ( uv_1));\n    float pixelHeight = depthMap.eval(depthMap_size * ( uv_1)).z;\n    float heightScale_1 = 0.5;\n    vec3 fragPos_1 = vec3(uv_1, pixelHeight * heightScale_1);\n    vec3 dirToLight = normalize(lightPos - fragPos_1);\n    vec3 currentPos = fragPos_1;\n    float stepSize = 0.0005000000237487256526947021484375;\n    float shadow = 1.0;\n    for (int i = 0; i < 640; i++)\n    {\n        currentPos += (dirToLight * stepSize);\n        bool _187 = any(lessThan(currentPos.xy, vec2(0.0)));\n        bool _196;\n        if (!_187)\n        {\n            _196 = any(greaterThan(currentPos.xy, vec2(1.0)));\n        }\n        else\n        {\n            _196 = _187;\n        }\n        if (_196)\n        {\n            break;\n        }\n        float heightVal = depthMap.eval(depthMap_size * ( currentPos.xy)).z * heightScale_1;\n        if (heightVal > (currentPos.z + 0.100000001490116119384765625))\n        {\n            shadow = clamp(shadow - 0.89999997615814208984375, 0.0, 1.0);\n            break;\n        }\n    }\n    vec3 diffuse_1 = FLT_flutter_local_calculateBasicLighting();\n    vec3 color = albedoPixel.xyz * ((diffuse_1 * shadow) + vec3(0.1500000059604644775390625, 0.100000001490116119384765625, 0.300000011920928955078125));\n    fragColor = vec4(color, albedoPixel.w);\n}\n\nhalf4 main(float2 iFragCoord)\n{\n      flutter_FragCoord = float4(iFragCoord, 0, 0);\n      FLT_main();\n      return fragColor;\n}\n",
    "stage": 1,
    "uniforms": [
      {
        "array_elements": 0,
        "bit_width": 0,
        "columns": 1,
        "location": 0,
        "name": "albedoMap",
        "rows": 1,
        "type": 12
      },
      {
        "array_elements": 0,
        "bit_width": 0,
        "columns": 1,
        "location": 1,
        "name": "depthMap",
        "rows": 1,
        "type": 12
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 2,
        "name": "screenSize",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 3,
        "name": "lightPos",
        "rows": 3,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 4,
        "name": "heightScale",
        "rows": 1,
        "type": 10
      }
    ]
  }
}